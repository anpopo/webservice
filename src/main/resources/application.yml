spring:
  profiles:
#    active: local
    include: oauth


  jpa:
    show_sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect

  h2:
    console:
      enabled: true


  session:
    store-type: jdbc